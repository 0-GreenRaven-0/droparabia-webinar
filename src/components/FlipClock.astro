---
interface Props {
  targetDate: string;
}
const { targetDate } = Astro.props;
---

<div class="countdown-widget flex items-center gap-3 lg:gap-3 font-[IBrand]">
  <div class="flex flex-col items-center">
    <span class="text-3xl lg:text-3xl text-white lg:text-black cd-days">00</span>
    <span class="text-xs text-white lg:text-black uppercase">Days</span>
  </div>
  <span class="text-2xl lg:text-2xl text-white lg:text-black mb-2 lg:mb-3">:</span>
  <div class="flex flex-col items-center">
    <span class="text-3xl lg:text-3xl text-white lg:text-black cd-hours">00</span>
    <span class="text-xs text-white lg:text-black uppercase">Hours</span>
  </div>
  <span class="text-2xl lg:text-2xl text-white lg:text-black mb-2 lg:mb-3">:</span>
  <div class="flex flex-col items-center">
    <span class="text-3xl lg:text-3xl text-white lg:text-black cd-minutes">00</span>
    <span class="text-xs text-white lg:text-black uppercase">Minutes</span>
  </div>
  <span class="text-2xl lg:text-2xl text-white lg:text-black mb-2 lg:mb-3">:</span>
  <div class="flex flex-col items-center">
    <span class="text-3xl lg:text-3xl text-white lg:text-black cd-seconds">00</span>
    <span class="text-xs text-white lg:text-black uppercase">Seconds</span>
  </div>
</div>

<script is:inline define:vars={{ targetDate }}>
  (function() {
    const target = new Date(targetDate).getTime();
    const pad = n => String(Math.max(0, n)).padStart(2, '0');

    function tick() {
      const diff = Math.max(0, target - Date.now());
      const d = pad(Math.floor(diff / 86400000));
      const h = pad(Math.floor((diff % 86400000) / 3600000));
      const m = pad(Math.floor((diff % 3600000) / 60000));
      const s = pad(Math.floor((diff % 60000) / 1000));

      document.querySelectorAll('.cd-days').forEach(el => el.textContent = d);
      document.querySelectorAll('.cd-hours').forEach(el => el.textContent = h);
      document.querySelectorAll('.cd-minutes').forEach(el => el.textContent = m);
      document.querySelectorAll('.cd-seconds').forEach(el => el.textContent = s);
    }

    tick();
    setInterval(tick, 1000);
  })();
</script>
